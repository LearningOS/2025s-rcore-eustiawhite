实现的功能：一个系统调用，能在指定地址写入或读取一个字节的内容，还能返回当前task下系统调用的次数，我维护了一个全局的二维数组来记录这些系统调用的次数

问答题：

第一个大题我打算之后再调试

第二个大题：
1.代表用户栈地址，第一种情况是一个程序执行了某条指令导致陷入S特权级，这种情况下调用__alltraps压入上下文；第二种情况是需要切换到下一个应用程序，这种情况下不调用__alltraps，直接压入上下文

2.sstatus：SPP 等字段给出 Trap 发生之前 CPU 处在哪个特权级（S/U）等信息 
sepc：当 Trap 是一个异常的时候，记录 Trap 发生之前执行的最后一条指令的地址 
sscratch：所有的通用寄存器不是需要被保存就是不能被改动，但是我们需要一个寄存器存放地址，于是利用了sscratch，它能存放内核栈地址赋给sp，也能在离开trap的时候把sp原本存放的用户栈地址还给sp

3.x2是sp，要基于它来找到每个寄存器应该被保存到的正确的位置；x4是tp，通常用不到，保存上下文的时候也没保存它

4.sp:用户栈地址，sscratch:内核栈地址

5.sret，硬件提供了状态的转换

6.sp:内核栈地址，sscratch:用户栈地址

7.ecall

荣誉准则：
1.在完成本次实验的过程（含此前学习的过程）中，我曾分别与 以下各位 就（与本次实验相关的）以下方面做过交流，还在代码中对应的位置以注释形式记录了具体的交流对象及内容：

讨论群的各位热心群友和贴心的助教，帮我查出了二维数组开太大会出问题的bug,
在我对docker一无所知的时候教我如何使用

2.此外，我也参考了 以下资料 ，还在代码中对应的位置以注释形式记录了具体的参考来源及内容：

主要就是turtial和GPT...

3.我独立完成了本次实验除以上方面之外的所有工作，包括代码与文档。 我清楚地知道，从以上方面获得的信息在一定程度上降低了实验难度，可能会影响起评分。

4.我从未使用过他人的代码，不管是原封不动地复制，还是经过了某些等价转换。 我未曾也不会向他人（含此后各届同学）复制或公开我的实验代码，我有义务妥善保管好它们。 我提交至本实验的评测系统的代码，均无意于破坏或妨碍任何计算机系统的正常运转。 我清楚地知道，以上情况均为本课程纪律所禁止，若违反，对应的实验成绩将按“-100”分计。
